test_command: echo "no tests required"
branch_prefix: ai/fix
pr_reviewers:
  - selva
pr_labels:
  - ai-generated
  - needs-review
claude_prompt_prefix: |
  You are fixing a bug in a node application.
  Follow existing code style and error handling patterns.
  Do not modify .env files, requirements.txt, or any infrastructure config.
  Keep changes minimal and focused on the reported issue only.
